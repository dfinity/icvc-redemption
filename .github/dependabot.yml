version: 2
updates:
  # Keep the SHA-pinned GitHub Actions in .github/workflows/ patched.
  # Dependabot reads the trailing "# v4" comment on each pin to know the
  # version family, and opens PRs that bump both the SHA and the comment.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
